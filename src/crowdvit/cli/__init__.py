"""Command-line entrypoints (train/evaluate/benchmark/export/make-manifest)
and the shared config-loading and dataloader-building plumbing they use.
The thin scripts in ``scripts/`` at the repo root just call into these.
"""
