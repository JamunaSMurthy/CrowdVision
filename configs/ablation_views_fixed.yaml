# Table: Adaptive temporal-view ablation — "Fixed temporal views" row.
# b_s(X) is replaced by a uniform weight vector (plain average pooling over
# each window) instead of being predicted from the clip descriptor.
_base_: base.yaml
model:
  view_mode: fixed
  view_window_sizes: [8, 4, 2]
